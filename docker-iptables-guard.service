[Unit]
Description=Reassert hotspot iptables invariants after Docker networking events
After=docker.service
Wants=docker.service

[Service]
Type=simple
ExecStart=/usr/local/sbin/docker-iptables-guard.sh
Restart=always
RestartSec=1

[Install]
WantedBy=multi-user.target